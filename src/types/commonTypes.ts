export type CommonTypes = {
  common:{
    isLoading : boolean
    isCall: boolean
  }
}

export type FollowListType = {
  profileImg:string
  userEmail:string
  followerEmail:string
  followTime:string
}