export type AlimTypes = {
    autoNo: number,
    sendMemNo: number,
    receiveMemNo: number,
    alimCode: number,
    insDate: string,
    postNo: number,
    sendName: string,
    sendEmail: string,
    alimCodeKor: string,
    sendProfileImg: string

}